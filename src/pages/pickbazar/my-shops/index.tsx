import { Box } from "@chakra-ui/react";
import CustomLayout from "../../../components/CustomLayout";
import Header from "../../../components/Header";

const MyShops = () => {
  return (
    <CustomLayout>
      <Header title="My Shops" subtitle="" report={""} />
    </CustomLayout>
  );
};

export default MyShops;
